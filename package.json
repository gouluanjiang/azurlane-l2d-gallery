{
  "name": "azurlane-l2d-gallery",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate-catalog.mjs --metadata-only",
    "validate:local": "node scripts/validate-catalog.mjs",
    "update:cloud": "node scripts/check-updates.mjs",
    "sync": "node scripts/sync.mjs",
    "preview": "node scripts/preview.mjs",
    "desktop:build": "node desktop/build.mjs",
    "desktop:dev": "electron ."
  },
  "devDependencies": {
    "@electron/packager": "20.3.0",
    "electron": "44.3.0"
  },
  "main": "desktop/main.cjs"
}
